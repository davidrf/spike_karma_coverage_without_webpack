describe('plusOne', function () {
  it('should add one to the given number', function () {
    var number = plusOne(1);
    expect(number).toBe(2);
  });

  it('should add one to the given number', function () {
    var number = plusTwo(1);
    expect(number).toBe(3);
  });
});
